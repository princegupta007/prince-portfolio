# Copy to .env.local for development. NEVER commit real .env files (gitignored).
# All variables here are PUBLIC (NEXT_PUBLIC_*) by design — this project has no secrets.

# Canonical site origin used for metadataBase/canonical/OG URLs (Phase 8).
# Leave unset locally and on Vercel previews — code falls back to VERCEL_URL there.
# Owner decision pending (Phase 0 open question #1); placeholder:
NEXT_PUBLIC_SITE_URL=

# Public contact email (same value lives in lib/constants.ts; kept here for
# convenience/tooling only — it is not a secret, it is printed on the CV).
NEXT_PUBLIC_CONTACT_EMAIL=princegupta98299@gmail.com
